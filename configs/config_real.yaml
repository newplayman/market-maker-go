env: production
risk:
  maxOrderValueUSDT: 100
  maxNetExposure: 1
gateway:
  apiKey: ${MM_GATEWAY_API_KEY}
  apiSecret: ${MM_GATEWAY_API_SECRET}
  baseURL: https://fapi.binance.com
symbols:
  ETHUSDC:
    tickSize: 0.01
    stepSize: 0.001
    minQty: 0.001
    maxQty: 8000
    minNotional: 20
    strategy:
      type: asmm
      minSpread: 0.0006
      quoteIntervalMs: 150
      minSpreadBps: 6
      maxSpreadBps: 40
      minSpacingBps: 4
      maxLevels: 3
      baseSize: 0.01
      sizeVolK: 0.5
      targetPosition: 0
      invSoftLimit: 3
      invHardLimit: 5
      invSkewK: 1.5
      volK: 0.8
      trendSpreadMultiplier: 1.5
      highVolSpreadMultiplier: 2.0
      avoidToxic: true
    risk:
      singleMax: 1
      dailyMax: 10
      netMax: 5
      latencyMs: 500
      pnlMin: -5
      pnlMax: 10
      reduceOnlyThreshold: 3
      stopLoss: -20
      haltSeconds: 30
      shockPct: 0.02